import styled from 'styled-components';

const Title = styled.div`
  width: 100%;
  color: black;
  font-size: 1.4rem;
  font-weight: bold;
  margin-bottom: 0.4rem;
  line-height: 1.5rem;
  height: 1.5rem;
  overflow: hidden;
`;

export default Title;
